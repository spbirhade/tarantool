# encoding: tarantool

import time
#
# clear statistics
server.restart()

print """#
# check stat_cleanup
#  add several tuples
#
"""
for i in range(10):
  exec sql "insert into t0 values ({0}, 'tuple')".format(i)

time.sleep(2)
exec admin "show stat"
print """#
# restart server
#
"""
server.restart()
print """#
# statistics must be zero
#
"""
time.sleep(2)
exec admin "show stat"
# vim: syntax=python
